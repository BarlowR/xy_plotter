import numpy as np



def OutlinePath(object):

	@staticmethod
	def outline_outer_image(img, prior_paths):
		#img is a numpy boolean array of a black and white image, where blac pixels are 1 and white pixels are 0
		#prior_paths is a list of directed graphs, with each element corresponding to a boundry outline in the original image
		start_vertex = (0,0)
		for row in img[1:;]
			for column in row:
				if 


